@typeparam Titem

@if (MyList is null)
{
	@if (Loading is null)
	{
		<Loading />
	}
	else
	{
		@Loading
	}
}
else if (MyList.Count == 0)
{
	@if (NoRecords is null)
	{
		<p><em>@Localizer["NoRecords"]</em></p>
	}
	else
	{
		@NoRecords
	}
}
else
{
	@Body
}